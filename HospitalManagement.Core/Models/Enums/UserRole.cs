/* Amy Zhang
Summary: This enum defines the different user roles
in the hospital management system. */

namespace HospitalManagement.Core.Models;

public enum UserRole
{
    Patient = 0,
    Doctor = 1,
    Admin = 2
}